/**
 * Entry point of the Expo app — registers the root component.
 * Required by Expo to properly mount the app on both iOS and Android.
 */
import { registerRootComponent } from 'expo';
import App from './src/App';

registerRootComponent(App);
