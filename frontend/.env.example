# =============================================================================
# MNESYA Frontend - Variables d'environnement (Expo)
# Copier ce fichier en .env.local pour le développement
# NE JAMAIS committer .env.local
# =============================================================================

# URL de l'API backend
# En dev local : remplacer par l'IP de votre machine (ifconfig getifaddr en0)
# En production : injecté automatiquement par Coolify
EXPO_PUBLIC_API_URL=http://localhost:8000
